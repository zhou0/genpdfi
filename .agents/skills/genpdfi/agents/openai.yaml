interface:
  display_name: "Genpdfi"
  short_description: "Repo-specific patterns and workflows for genpdfi"
  default_prompt: "Use the genpdfi repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true